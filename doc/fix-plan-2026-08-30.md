# TG-SignPulse 合并修复计划（2026-08-30）

> 依据两份评审合并产出：
> - [doc/review-upstream-0.9-optimization.md](review-upstream-0.9-optimization.md)（0.9 同步兼容性）
> - [doc/第一轮GPT-review.md](第一轮GPT-review.md)（升级整体安全/质量）
>
> 状态标记：**已修** ✅ / **待修** ⏳ / **评估后暂缓** 🔶 / **不适用** ⛔（已实证推翻）

## 第一档：正确性 + 日志安全（本次修复，P0）

| # | 来源 | 位置 | 修复项 | 状态 |
|---|------|------|--------|------|
| 1 | upstream 2.2 | `tg_signer/core.py` `sign_once` | 只清当前 route_key 的消息缓存，不做全量清理（0.9 语义，合并引入回归） | ✅ 已修 |
| 2 | upstream 3.2 | `tg_signer/core.py` `close_client_by_name` | 双 key（基础 key + `::memory`）早退改为 `continue`，确保两个 key 都被评估 | ✅ 已修 |
| 3 | upstream 2.3 | `tg_signer/config.py` + `core.py` | `BaseJSONConfig.load` 递归遍历完整 V3→V2→V1 链；`load_config` 对返回 `None` 给出可读报错而非裸 `TypeError` | ✅ 已修 |
| 4a | GPT 3.5 | `backend/services/tasks.py` `cleanup_old_logs` | 删除前校验路径位于 `resolve_logs_dir()` 内，拒绝符号链接/非普通文件 | ✅ 已修 |
| 4b | GPT 3.3 | `backend/api/routes/tasks.py` `get_log_output` | 完整日志读取限制大小（尾读 + 上限），返回 `truncated` 标记 | ✅ 已修 |

## 第二档：安全加固（本次修复，P0）

| # | 来源 | 位置 | 修复项 | 状态 |
|---|------|------|--------|------|
| 5 | GPT 2.1 | `docker-compose.yml` + `backend/core/config.py` | 删除固定默认 JWT 密钥；显式配置已知弱值/过短（<32）密钥时拒绝启动，未配置自动生成持久化强密钥 | ✅ 已修 |
| 6 | GPT 2.3 | `backend/utils/tg_session.py` + `tg_signer/core.py` | session_string 文件写入设 `0o600`，会话目录设 `0o700` | ✅ 已修 |
| 7 | GPT 2.2 | `backend/api/routes/tasks.py` / `sign_tasks_v2.py` + 前端 | WebSocket 改为首帧携带 token 认证（不再放 URL 查询参数），10s 内未发/无效即 1008 关闭 | ✅ 已修 |
| 8 | GPT 4.4 | `backend/api/routes/config.py` 等 4 个路由 | 未知异常 `str(e)` 全部移除：服务端 `logger.exception` 记完整异常，客户端返回稳定错误码；业务 `ValueError`（如"密码错误"）保留用户可读消息 | ✅ 已修 |

## 第三档：架构收敛（待修，长期）

| # | 来源 | 位置 | 修复项 | 状态 |
|---|------|------|--------|------|
| 9 | GPT 3.4 + upstream 2.4/4.2 | `backend/services/tasks.py` / `sign_tasks.py` | 统一账号级执行锁（跨进程）+ 每次运行生成 `run_id` | ✅ 已修（`account_locks.py` 升级为 `AccountLock`：asyncio.Lock 子类 + 可选 fcntl 文件锁，env `ACCOUNT_LOCK_FILE` 开启，默认关闭行为不变；`start_task_run`/`_save_run_info` 每次运行生成并持久化 `run_id`，history 条目含 `run_id`；legacy `TaskLog` 增 `run_id` 列 + 幂等迁移 `ensure_schema_upgrades`） |
| 10 | GPT 3.4 | `backend/services/*` | 任务状态从内存布尔值升级为持久化状态机（pending→running→success/failed/cancelled） | ✅ 已修（`sign_tasks` 运行状态 `_run_statuses` 从内存升级为「内存缓存 + 磁盘 JSON 持久化」：`_set_run_status` 原子写盘到 `history/_run_status/`，启动时 `_load_persisted_run_statuses` 恢复，重启前 `running` 态标记 `cancelled`；legacy `TaskLog.status` 本就 DB 持久化，补 `run_id`） |
| 11 | GPT 3.1 | `backend/` | 明确单用户模型并写入部署文档（或补 owner_id） | ✅ 已修（README.md / README_EN.md 新增「单用户模式」章节；不做 owner_id 改造，见 B） |
| 12 | GPT 4.1 | `backend/` | ruff E402 16 处：上移 import 或局部 `# noqa`，CI 纳入 `ruff check` | ✅ 已修（16 处清零；新增 `.github/workflows/lint-test-build.yml` 含 ruff） |
| 13 | GPT 4.3 | CI | 前端生产构建纳入 CI 门禁（vue-tsc + vite build） | ✅ 已修（CI 新增 frontend job） |
| 14 | upstream 6.x | `tg_signer/core.py` | 面板 adapter 分层收敛，建立上游同步裁决表（0.10 升级减冲突） | ✅ 已修（新增 `doc/upstream-sync-decision-table.md`：模块裁决表 + 9 项待裁决风险点（P1/P2，含 SPA 穿越、tg_session 单进程锁、atomic_write_json 权限等）+ 同步流程 + adapter 分层目标；`doc/review-upstream-0.9-optimization.md` §6.3 模板落地） |

## 已实证推翻 / 不适用

| # | 来源 | 论断 | 处置 |
|---|------|------|------|
| A | GPT 4.2 | 前端无法生产构建 / lockfile 无效 | ⛔ 已实测 `npm ci` + `npm run build`（vue-tsc+vite+PWA）成功；GPT 环境 `NODE_ENV=production` 省略 dev 依赖所致。lockfile 有效、Docker 链路可行 |
| B | GPT 3.1 | 多用户越权风险 | 🔶 现状为单用户管理模式，写入部署文档声明即可，不做 owner_id 改造 |
| C | GPT 2.2 | WebSocket 查询参数泄密 | ✅ 已在第二档修复（首帧认证，token 不再入 URL）；反向代理日志脱敏仍建议在部署侧开启 |

## 回归测试（第一档 ✅ 已落地 / 第二档 ✅ 已落地）

**第一档：**
- 多 chat：A chat 完成不清空 B chat 消息缓存（`test_normal_run_clears_only_current_chat_route_key`）✅
- `close_client_by_name` 基础 key 与 `::memory` key 都能关闭（`test_close_client_by_name_cleans_both_keys_even_when_base_in_use`）✅
- V1→V3 直接迁移成功且写回；非法配置返回可读 ValueError（`TestSignConfigV3LoadRecursiveChain` 6 例 + `test_load_config_raises_readable_error_on_unknown_config`）✅
- 日志清理路径越界/符号链接拒绝；完整日志读取截断（`test_cleanup_old_logs_rejects_outside_logs_dir` / `test_get_log_output_truncates_large_file`）✅

**第二档：**
- 弱 JWT 密钥（占位/过短）拒绝，强密钥/自动生成不受影响（`test_weak_secret_key_rejected`）✅
- session_string 文件 0o600 / 会话目录 0o700（`test_session_string_file_permissions_0o600`）✅
- WS 首帧认证：缺 token / 无效 token → 1008；有效 token → 正常推送（`test_ws_first_frame_auth_*` 3 例）✅
- 未知异常响应稳定不泄露 `str(e)`（`test_config_error_responses_are_stable` / `test_get_log_output_error_response_stable`）✅

**验证：** 修复后 `pytest tests` **184 通过**（第一档后 177 + 第二档新增 7）；`ruff check tg_signer/` 全绿；改动文件 `ruff format --check` 通过（后端存量 E402 6 处属第三档 12）；前端 `npm run build`（vue-tsc + vite + PWA）成功 ✅

> 验证命令：`pytest tests -o addopts="-W=ignore::DeprecationWarning:pyrogram.utils"`；`ruff check tg_signer/` 全绿。

## 第三档后续：发布前整改（第一轮GPT-review.md / 第二轮GPT-review.md，2026-08-30 ✅）

> 依据 `doc/第二轮GPT-review.md` 发布前 6 项 + 可落地后续项全部落地。

| # | 级别 | 修复项 | 落地 |
|---|---|---|---|
| 3.1 | P1 | DB task（`run_task_once`）接入统一 `AccountLock`，覆盖创建记录→启动子进程→等待完成 | ✅ `_run_task_once_locked` + 锁超时写失败记录（`test_tasks_account_lock.py` 2 例） |
| 3.2 | P1 | 跨进程文件锁可取消/超时（`LOCK_NB` 轮询 + `ACCOUNT_LOCK_TIMEOUT` + `AccountLockTimeout`） | ✅ 非阻塞轮询，响应取消，超时释放进程内锁（`test_account_locks.py` +2 例） |
| 3.3 | P1 | README/README_EN/.env.example 弱密钥示例修正 | ✅ 改为 `openssl rand -hex 32` 与必填说明 |
| 3.4 | P1 | CI 前端 `npm ci --include=dev` + 移除 `NODE_ENV=development` 矛盾 | ✅ 已改 CI，本地同款命令构建成功 |
| 5.3/2 | — | 生产默认开启跨进程锁 + 启动日志/README 说明 | ✅ docker-compose `ACCOUNT_LOCK_FILE=1`；启动打印锁模式；README 增 `ACCOUNT_LOCK_FILE`/`ACCOUNT_LOCK_TIMEOUT` |
| 5.1/11 | — | 密钥持久化失败明显处理 | ✅ 写失败生产模式拒绝启动（RuntimeError）、开发模式告警；密钥文件 0600（`test_auto_generated_secret_key_0600_and_production_write_failure`） |
| 5.2/7 | — | DB task 状态机统一（run_id+state+worker_id） | ✅ TaskLog 增 `worker_id` 列（幂等迁移）+ SignTask 状态同带 `worker_id`，两侧对齐 |
| 5.4/12 | — | 前端 glob 依赖审计 | ✅ `npm audit` 0 漏洞；`glob@11.1.0` deprecated 来自 workbox-build（vite-plugin-pwa 构建链，仅 dev），升级需等 workbox-build 换代 |
| 8 | — | Web 调度对子进程 CLI 依赖收敛 | 🔶 评估文档已写入 `doc/upstream-sync-decision-table.md` §5；进程内化属行为级重构，后续迭代推进（当前互斥已由 AccountLock 闭环） |

**验证：** `pytest tests` **199 通过**（第三档后 194 + 本次新增 5）；`ruff check backend/ tg_signer/` 全绿；前端 `npm ci --include=dev` + `npm run build`（vue-tsc+vite+PWA）成功。

## 第三轮 GPT Review（doc/第三轮GPT-review.md，发布候选 🟢）

> 结论：发布前整改已基本闭环，**当前版本可进入 Release Candidate 阶段**；
> 剩余均为非阻塞事项。review 文档体系已整理为 第一/二/三轮 编号（旧
> `GPT-review-20260630*.md` 更名）。

| # | 非阻塞事项 | 处置 |
|---|---|---|
| 4.1 | 裁决表仍有过时构建命令 `NODE_ENV=development npm run build` | ✅ 已修：`doc/upstream-sync-decision-table.md` §3 改为 `npm ci --include=dev && npm run build`（生产构建，无需 NODE_ENV） |
| 4.2 | Web 调度保留 CLI 子进程轨道 | ✅ 已修：`run_task_once` 统一走进程内 `SignTaskService.run_task_with_logs`（`lock_already_held=True` 复用外层锁 + 传 `run_id` 对齐状态机）；`backend/cli/tasks.py` 的 `async_run_task_cli` 仅保留命令行兼容入口，Web 调度不再依赖 |
| 4.3 | 真实部署验证（Docker/Telegram/多 worker/重启恢复等） | 🔶 代码级已完成：Docker 干净构建（`docker build` + compose 冒烟）在本环境验证中；真实 Telegram/多账号/反向代理 WebSocket 等仍需目标环境，见 `doc/deployment-verification.md` |
| 5-1 | 统一 DB task 与 SignTask 持久化状态模型 | ✅ 已修：DB task 执行进入 SignTask 状态机（`manage_run_status` 写 running/finished），TaskLog 与状态机/历史 `run_id` 对齐；`worker_id` 两侧一致 |
| 5-2 | 面板历史记录统一 SQLite 主存储 | ✅ 已修：新增 `backend/models/task_run_history.py` + `backend/services/run_history.py`（`<workdir>/history/run_history.sqlite3`）；`_save_run_info` 写 SQLite 主存储，读取优先 SQLite 回退 JSON；删除/清空/按账号清空同步 |
| 5-3 | 多 worker + 真实 Telegram 回归测试 | ✅ 代码级已修：`tests/test_multi_worker.py` 跨进程双 worker 同账号串行互斥 + worker_id 可区分；真实 Telegram 环境回归仍属 4.3 目标环境项 |

**验证（第三轮 review 复跑 + 本轮）：** `pytest tests` **210 通过**（第三轮 199 + 本轮新增 11）；`ruff check backend/ tg_signer/ tests/` 全绿；Docker 干净构建 + compose 冒烟见 `doc/deployment-verification.md`；前端构建（vue-tsc+vite+PWA）成功。
