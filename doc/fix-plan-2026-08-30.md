# TG-SignPulse 合并修复计划（2026-08-30）

> 依据两份评审合并产出：
> - [doc/review-upstream-0.9-optimization.md](review-upstream-0.9-optimization.md)（0.9 同步兼容性）
> - [doc/GPT-review-20260630.md](GPT-review-20260630.md)（升级整体安全/质量）
>
> 状态标记：**已修** ✅ / **待修** ⏳ / **评估后暂缓** 🔶 / **不适用** ⛔（已实证推翻）

## 第一档：正确性 + 日志安全（本次修复，P0）

| # | 来源 | 位置 | 修复项 | 状态 |
|---|------|------|--------|------|
| 1 | upstream 2.2 | `tg_signer/core.py` `sign_once` | 只清当前 route_key 的消息缓存，不做全量清理（0.9 语义，合并引入回归） | ✅ 已修 |
| 2 | upstream 3.2 | `tg_signer/core.py` `close_client_by_name` | 双 key（基础 key + `::memory`）早退改为 `continue`，确保两个 key 都被评估 | ✅ 已修 |
| 3 | upstream 2.3 | `tg_signer/config.py` + `core.py` | `BaseJSONConfig.load` 递归遍历完整 V3→V2→V1 链；`load_config` 对返回 `None` 给出可读报错而非裸 `TypeError` | ✅ 已修 |
| 4a | GPT 3.5 | `backend/services/tasks.py` `cleanup_old_logs` | 删除前校验路径位于 `resolve_logs_dir()` 内，拒绝符号链接/非普通文件 | ✅ 已修 |
| 4b | GPT 3.3 | `backend/api/routes/tasks.py` `get_log_output` | 完整日志读取限制大小（尾读 + 上限），返回 `truncated` 标记 | ✅ 已修 |

## 第二档：安全加固（本次修复，P0）

| # | 来源 | 位置 | 修复项 | 状态 |
|---|------|------|--------|------|
| 5 | GPT 2.1 | `docker-compose.yml` + `backend/core/config.py` | 删除固定默认 JWT 密钥；显式配置已知弱值/过短（<32）密钥时拒绝启动，未配置自动生成持久化强密钥 | ✅ 已修 |
| 6 | GPT 2.3 | `backend/utils/tg_session.py` + `tg_signer/core.py` | session_string 文件写入设 `0o600`，会话目录设 `0o700` | ✅ 已修 |
| 7 | GPT 2.2 | `backend/api/routes/tasks.py` / `sign_tasks_v2.py` + 前端 | WebSocket 改为首帧携带 token 认证（不再放 URL 查询参数），10s 内未发/无效即 1008 关闭 | ✅ 已修 |
| 8 | GPT 4.4 | `backend/api/routes/config.py` 等 4 个路由 | 未知异常 `str(e)` 全部移除：服务端 `logger.exception` 记完整异常，客户端返回稳定错误码；业务 `ValueError`（如"密码错误"）保留用户可读消息 | ✅ 已修 |

## 第三档：架构收敛（待修，长期）

| # | 来源 | 位置 | 修复项 | 状态 |
|---|------|------|--------|------|
| 9 | GPT 3.4 + upstream 2.4/4.2 | `backend/services/tasks.py` / `sign_tasks.py` | 统一账号级执行锁（跨进程）+ 每次运行生成 `run_id` | ⏳ |
| 10 | GPT 3.4 | `backend/services/*` | 任务状态从内存布尔值升级为持久化状态机（pending→running→success/failed/cancelled） | ⏳ |
| 11 | GPT 3.1 | `backend/` | 明确单用户模型并写入部署文档（或补 owner_id） | 🔶 |
| 12 | GPT 4.1 | `backend/` | ruff E402 16 处：上移 import 或局部 `# noqa`，CI 纳入 `ruff check` | ⏳ |
| 13 | GPT 4.3 | CI | 前端生产构建纳入 CI 门禁（vue-tsc + vite build） | ⏳ |
| 14 | upstream 6.x | `tg_signer/core.py` | 面板 adapter 分层收敛，建立上游同步裁决表（0.10 升级减冲突） | ⏳ |

## 已实证推翻 / 不适用

| # | 来源 | 论断 | 处置 |
|---|------|------|------|
| A | GPT 4.2 | 前端无法生产构建 / lockfile 无效 | ⛔ 已实测 `npm ci` + `npm run build`（vue-tsc+vite+PWA）成功；GPT 环境 `NODE_ENV=production` 省略 dev 依赖所致。lockfile 有效、Docker 链路可行 |
| B | GPT 3.1 | 多用户越权风险 | 🔶 现状为单用户管理模式，写入部署文档声明即可，不做 owner_id 改造 |
| C | GPT 2.2 | WebSocket 查询参数泄密 | ✅ 已在第二档修复（首帧认证，token 不再入 URL）；反向代理日志脱敏仍建议在部署侧开启 |

## 回归测试（第一档 ✅ 已落地 / 第二档 ✅ 已落地）

**第一档：**
- 多 chat：A chat 完成不清空 B chat 消息缓存（`test_normal_run_clears_only_current_chat_route_key`）✅
- `close_client_by_name` 基础 key 与 `::memory` key 都能关闭（`test_close_client_by_name_cleans_both_keys_even_when_base_in_use`）✅
- V1→V3 直接迁移成功且写回；非法配置返回可读 ValueError（`TestSignConfigV3LoadRecursiveChain` 6 例 + `test_load_config_raises_readable_error_on_unknown_config`）✅
- 日志清理路径越界/符号链接拒绝；完整日志读取截断（`test_cleanup_old_logs_rejects_outside_logs_dir` / `test_get_log_output_truncates_large_file`）✅

**第二档：**
- 弱 JWT 密钥（占位/过短）拒绝，强密钥/自动生成不受影响（`test_weak_secret_key_rejected`）✅
- session_string 文件 0o600 / 会话目录 0o700（`test_session_string_file_permissions_0o600`）✅
- WS 首帧认证：缺 token / 无效 token → 1008；有效 token → 正常推送（`test_ws_first_frame_auth_*` 3 例）✅
- 未知异常响应稳定不泄露 `str(e)`（`test_config_error_responses_are_stable` / `test_get_log_output_error_response_stable`）✅

**验证：** 修复后 `pytest tests` **184 通过**（第一档后 177 + 第二档新增 7）；`ruff check tg_signer/` 全绿；改动文件 `ruff format --check` 通过（后端存量 E402 6 处属第三档 12）；前端 `npm run build`（vue-tsc + vite + PWA）成功 ✅

> 验证命令：`pytest tests -o addopts="-W=ignore::DeprecationWarning:pyrogram.utils"`；`ruff check tg_signer/` 全绿。
